/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Commit SHA injected at build time (empty string for local builds). */
declare const __COMMIT_SHA__: string;
